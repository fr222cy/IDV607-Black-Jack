//We tried to use a base-class for the rules, but we didnt manage to get it to work.

////using System;
////using System.Collections.Generic;
////using System.Linq;
////using System.Text;

////namespace BlackJack.model.rules
////{
////    class NewGameBaseStrategy
////    {

////        public NewGameBaseStrategy(Deck a_deck, Dealer a_dealer, Player a_player)
////        {
////            Card c;

////            c = a_deck.GetCard();
////            c.Show(true);
////            a_player.DealCard(c);

////            c = a_deck.GetCard();
////            c.Show(true);
////            a_dealer.DealCard(c);

////            c = a_deck.GetCard();
////            c.Show(true);
////            a_player.DealCard(c);

////        }

////    }
////}
